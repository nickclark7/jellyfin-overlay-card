{
  "name": "jellyfin-overlay-card",
  "version": "0.3.1",
  "description": "Home Assistant Lovelace card that pops out a Jellyfin web client full-screen over the dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit",
    "build": "npm run typecheck && node esbuild.config.mjs"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild-wasm": "0.28.1",
    "typescript": "^5.7.2"
  }
}
